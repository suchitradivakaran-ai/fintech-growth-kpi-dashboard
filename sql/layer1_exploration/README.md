## Layer 1: Data Familiarisation

This layer focuses on understanding the structure of the Google Analytics session dataset.
Queries in this folder are exploratory and are used to inspect top-level fields, nested hit-level data,
and different hit types (pageviews, events, transactions).

These queries help build context before any aggregation or KPI computation is performed.
Outputs from this layer are not used directly in dashboards.

